# UI Audit Report - 2026-05-06

## 1) Ket qua kiem tra nhanh

- Frontend build: `PASS` (`npm run build` tai root).
- Backend build: `PASS` (`npm run build` tai `backend/`).
- Unit tests: `PASS` (9 test files, 24 tests).
- Lint toan repo: `FAIL` (101 van de: 76 errors, 25 warnings).
  - Luu y: phan lon lint errors nam o backend va mot so file UI khac, khong chi rieng man vua sua.

## 2) Loi / rui ro dang ton tai

- **Lint blocking tren toan repo**
  - Nhieu loi `@typescript-eslint/no-explicit-any` trong backend va frontend.
  - Co loi `no-empty`, `no-empty-object-type`, `no-require-imports`.
  - Co canh bao hook dependencies (`react-hooks/exhaustive-deps`) o dashboard tabs.
- **Phu thuoc backend runtime**
  - Neu backend khong chay se xuat hien `ECONNREFUSED` tu Vite proxy cho `/api/v1/*`.

## 3) Cac man chua hoan thien (theo code hien tai)

### 3.1 Dashboard (`/`) - chua dong bo du lieu that 100%

- `src/components/dashboard/tabs/OverviewTab.tsx`
  - Bang hop dong van fallback sang mock `contractsData` neu khong co du lieu API.
- `src/components/dashboard/tabs/ProjectTab.tsx`
  - Dang dung mock co dinh cho bang:
    - `contractsData`
    - `handoversData`
    - `trainingsData`
- `src/components/dashboard/tabs/MaterialTab.tsx`
  - Dang dung du lieu mock thiet bi `equipmentData`.
- Nguon mock dang duoc dung:
  - `src/data/tableData.ts`
  - `src/data/equipmentData.ts`
  - `src/data/dashboardData.ts` (generator/fallback dashboard)

**Tac dong:** so lieu dashboard co the khong trung khop backend theo thoi gian thuc.

---

### 3.2 Chi tiet hop dong (`/hop-dong` -> xem chi tiet) - noi dung mo phong

- `src/components/details/ContractDetailDialog.tsx`
  - Tab "Dieu khoan chinh": noi dung hardcoded.
  - Tab "Danh muc san pham": dung `sampleProducts(...)` sinh du lieu mo phong.
  - Tab "Tai lieu": dung `sampleDocs` hardcoded.
  - Tab "Dao tao & Huan luyen": dung `sampleTraining` hardcoded.

**Tac dong:** man chi tiet hien du lieu "dep" nhung chua phan anh du lieu that tu backend.

---

### 3.3 Bao cao (`/bao-cao`) - fallback khi API rong/loi

- `src/pages/Reports.tsx`
  - Su dung fallback arrays:
    - `fallbackContractByCustomer`
    - `fallbackMonthlyTrend`
    - `fallbackUnitPerformance`
  - Co `contractStatus` static mau.

**Tac dong:** khi API tra ve rong/khong du, man van render duoc nhung so lieu la fallback.

---

### 3.4 Chat luong code chua dong deu cho UAT release

- Co nhieu lint errors o backend services/controllers, va mot phan frontend (`Training`, `TrainingDetail`, `scanner`, `ui` components).
- Van de nay khong nhat thiet gay trang man ngay, nhung la no ky thuat can xu ly truoc release production.

## 4) De xuat uu tien xu ly

1. **Uu tien cao:** bo mock o dashboard tabs (`ProjectTab`, `MaterialTab`, fallback `OverviewTab`) va lay du lieu API that.
2. **Uu tien cao:** thay du lieu mo phong trong `ContractDetailDialog` bang API detail.
3. **Uu tien trung binh:** chuan hoa man `Reports` de giam fallback static, hien ro trang thai "khong co du lieu".
4. **Uu tien trung binh:** xu ly lint errors nhom `any` va hook dependencies.
5. **Uu tien thap:** warning `Browserslist` (co the cap nhat sau).

## 5) Ghi chu

- Bao cao nay phan anh trang thai tai thoi diem kiem tra.
- Cac module chinh van co the chay/UAT duoc, nhung mot so man dang ket hop du lieu that + du lieu mo phong.
