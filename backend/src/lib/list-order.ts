/** Thứ tự danh sách mặc định: bản ghi mới tạo / cập nhật lên đầu. */
export const ORDER_BY_CREATED_DESC = { createdAt: "desc" as const };
export const ORDER_BY_UPDATED_DESC = { updatedAt: "desc" as const };
